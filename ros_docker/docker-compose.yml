version: "3.9"

services:
  ros:
    image: ros_humble_env
    container_name: ros_humble
    build: .
    volumes:
      - ./src/your_ros_ws:/ros_ws
    network_mode: host
    tty: true
    stdin_open: true
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
